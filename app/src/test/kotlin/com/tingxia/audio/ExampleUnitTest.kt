package com.tingxia.audio

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * 单测占位 — CP4.3 起补充 UI / ViewModel / Repository 用例
 */
class ExampleUnitTest {

    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
